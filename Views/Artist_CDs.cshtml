@if(Model.Count == 0) {
  <h1>None found</h1>
  <p><a href="/artist_search">search again</a></p>
} else {
@foreach (var cd in Model)
{
  <h1>@cd.CDArtist</h1>
  <ol>
      <li>@cd.CDName</li>
  </ol>
}
}
